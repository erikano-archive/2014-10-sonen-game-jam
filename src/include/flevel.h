#ifndef FLEVEL_H_
#define FLEVEL_H_

#include "cgame.h"

bool level1 (stats_level*, player*, player*);
bool intro2 (stats_level*, player*, player*);
bool level2 (stats_level*, player*, player*);
bool level3 (stats_level*, player*, player*);
bool level4 (stats_level*, player*, player*);
bool level5 (stats_level*, player*, player*);
bool outro5 (stats_level*, player*, player*);

#endif
